PORT=3000
HOST=0.0.0.0

DB_HOST=localhost
DB_PORT=3306
DB_NAME=album_copa
DB_USER=album_user
DB_PASSWORD=change-me

# Use true for managed cloud databases like Azure Database for MySQL.
DB_SSL=false
DB_SSL_REJECT_UNAUTHORIZED=true
# Optional when your provider gives a CA certificate path inside the container/host.
# DB_SSL_CA_PATH=/path/to/ca.pem
